import { AbstractCrudService } from './abstract-crud-service';

describe('AbstractCrudService', () => {
  it('should create an instance', () => {
    expect(new AbstractCrudService()).toBeTruthy();
  });
});
